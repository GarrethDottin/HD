{{#tag}}
  <div class="box">
    <div data-icon="ei-tag" data-size="s" class="box__icon"></div>
    <div class="box__body">
      <h4 class="box__title">{{name}}</h4>
      {{#if description}}
      <p class="box__text">
        {{description}}
      </p>
      {{/if}}
    </div>
  </div>
{{/tag}}